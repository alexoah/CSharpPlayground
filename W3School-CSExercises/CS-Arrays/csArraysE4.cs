/**
 *	from C# Arrays: Exercise 4 ( https://www.w3schools.com/cs/exercise.asp?filename=exercise_arrays4 )
 *
 *	question:
 *	Loop through the elements in the cars array using the foreach loop.
 *
 *	string[] cars = {"Volvo", "BMW", "Ford", "Mazda"};
 *	foreach (______ i in ____)
 *	{
 *		Console.WriteLine(_);
 *	}
 *
 */
string[] cars = {"Volvo", "BMW", "Ford", "Mazda"};
foreach (string i in cars) 
{
	Console.WriteLine(i);
}